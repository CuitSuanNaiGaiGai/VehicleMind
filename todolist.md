# VehicleMind Resume-Grade Project TODO

> 项目定位：构建一个可复现、可量化、可解释的“车载感知 → 语义上下文 → 安全事件 → Agent → 受控工具执行”系统，用于证明感知算法、模型部署、Agent 可靠性和系统工程能力。
>
> 使用规则：完成任务并通过对应验收条件后，才将 `[ ]` 改为 `[x]`。所有简历数字必须能够由仓库中的脚本、配置和原始结果复现。

## 0. 最终完成标准

- [ ] 从全新环境按 README 可以完成安装、模型准备、测试、离线评测和 Demo 运行。
- [ ] 感知部分不只调用预训练模型，至少包含一个自行训练、对比和消融的时序驾驶员状态模型。
- [ ] 道路感知具有公开数据集精度评测、ONNX 一致性验证和硬件性能测试。
- [ ] Agent 具有独立评测集、自动评分、执行轨迹、消融实验和确定性安全门。
- [ ] 系统提供可重复的端到端场景回放，不依赖摄像头或在线 LLM 才能完成回归测试。
- [ ] 核心逻辑自动化测试覆盖率达到 80% 以上。
- [ ] 所有关键结论同时报告成功案例、失败案例、限制条件和硬件环境。
- [ ] 发布带版本号的 GitHub Release，包含演示视频、结果摘要和复现说明。
- [ ] 项目中的预训练模型、数据集、第三方代码和生成资产均有来源与许可证说明。

## 1. 仓库与工程基线

### 需要完成

- [x] 建立功能分支与 Pull Request 工作流，禁止继续把未验证功能直接堆叠到 `main`。
- [x] 为 `main` 启用分支保护，要求 CI 通过后才能合并。
- [x] 补全 Python 依赖声明，并生成可锁定版本的环境文件。
- [x] 明确支持的 Python、macOS、Apple Silicon 和 CPU fallback 版本范围。
- [x] 配置 pytest 只收集 `tests/`，避免把 Demo 文件误识别为测试。
- [x] 修复 `face_present`/`presence` 等已经失效的 Demo 接口。
- [x] 将 `eye_closed` 统一为 `bool | None`，明确 `None` 表示当前没有可靠眼睛观测。
- [x] 删除、实现或解释仓库中的空模块、空测试和空配置文件。
- [ ] 将硬编码阈值迁移至可版本化配置文件，并保存每次实验使用的配置。
- [x] 增加 `.env.example`，确保密钥和本地路径不进入 Git。
- [x] 增加模型资产清单，记录文件名、来源、版本、SHA-256、许可证和预期路径。
- [ ] 增加 `LICENSE`、`THIRD_PARTY_NOTICES.md` 和贡献说明。
- [x] 建立 GitHub Actions，执行格式检查、静态检查、单元测试和核心离线 smoke test。

### 达到程度

- [x] 全新 clone 后不依赖开发者本机隐式环境即可安装。
- [x] `pytest` 能正常完成收集并全部通过。
- [x] 不连接摄像头、不下载大模型、不调用在线 API 时，核心逻辑测试仍可运行。
- [x] CI 在每个 Pull Request 上自动执行，失败时不能合并。
- [x] README 中的命令与真实入口一致，不存在已失效示例。

## 2. 数据、标注与实验协议

### 需要完成

- [ ] 确定驾驶员疲劳/分心数据集，并记录数据来源、许可证和使用限制。
- [ ] 建立统一标注格式，至少覆盖 `NORMAL`、`DROWSY`、`DISTRACTED`、`UNKNOWN` 和事件起止时间。
- [ ] 将 EAR、MAR、PERCLOS、闭眼时长、眨眼、哈欠、头姿和手机状态保存为可复用时序特征。
- [ ] 实现确定性的数据切分脚本，固定随机种子并保存 split manifest。
- [ ] 采用跨被试切分；同一人的相邻帧或相邻片段不得同时进入训练集和测试集。
- [ ] 为道路感知确定公开评测集和评测子集，并记录下载与预处理方式。
- [ ] 为端到端场景建立可回放的 context/event/tool trace 数据格式。
- [ ] 对数据类别分布、缺失值、片段时长和标签噪声生成统计报告。

### 达到程度

- [ ] 任意实验结果能够追溯到数据版本、split manifest、配置文件和 Git commit。
- [ ] 数据报告包含每个 split 的被试数、视频数、总时长和类别比例。
- [ ] 明确证明测试集与训练集不存在按帧泄漏和按被试泄漏。
- [ ] 公开仓库中不提交无授权原始数据，只提交下载说明、manifest 和必要的小型样例。

## 3. 舱内感知算法

### 3.1 可复现规则基线

- [ ] 将现有阈值式疲劳判定整理为正式 baseline。
- [ ] 固化眼睛、嘴部、PERCLOS、驾驶员存在和状态机的输入输出契约。
- [ ] 为短时人脸丢失、遮挡、快速转头和无效 landmark 建立明确的 UNKNOWN 处理规则。
- [ ] 为 Blink、PERCLOS、Yawn、Presence 和 DriverState 编写边界条件单元测试。

### 3.2 时序模型

- [ ] 实现基于固定时间窗口的特征序列数据集。
- [ ] 实现一个传统机器学习基线，用于证明时序深度模型的真实增益。
- [ ] 实现 GRU 时序分类模型。
- [ ] 实现 TCN 时序分类模型，并与 GRU 使用相同数据协议比较。
- [ ] 输出类别概率或置信度，并进行置信度校准分析。
- [ ] 将最佳模型导出为 ONNX，并接入 `CabinPerceptionService`。
- [ ] 保留规则模型 fallback，在时序模型不可用或输入质量不足时安全降级。

### 3.3 对比、消融与鲁棒性

- [ ] 比较规则基线、传统机器学习、GRU 和 TCN。
- [ ] 完成去除 PERCLOS 的消融实验。
- [ ] 完成去除头姿的消融实验。
- [ ] 完成去除哈欠/嘴部特征的消融实验。
- [ ] 完成去除手机/分心特征的消融实验。
- [ ] 按白天/夜间、遮挡、眼镜、快速转头和低质量画面分桶评测。
- [ ] 记录 False Positive、False Negative 和检测延迟最大的典型失败案例。

### 达到程度

- [ ] 跨被试测试集 Macro-F1 目标不低于 0.80。
- [ ] DROWSY 事件 Recall 目标不低于 0.85。
- [ ] 疲劳事件中位检测延迟目标不超过 2 秒。
- [ ] 正常驾驶误报目标不超过 1 次/小时。
- [ ] 同时报告 Precision、Recall、F1、混淆矩阵、ROC/PR 曲线和每类样本量。
- [ ] 最终模型相对规则基线的增益具有可解释的对比或消融证据。
- [ ] 模型导出前后关键指标下降不超过 1 个百分点。

## 4. 道路感知与模型部署

### 4.1 精度评测

- [ ] 建立 YOLOPv2 目标检测、车道分割和可行驶区域分割的统一离线评测入口。
- [ ] 检查类别映射、坐标缩放、letterbox、mask resize 和 NMS 的正确性。
- [ ] 对 PyTorch/参考实现与 ONNX 输出进行数值及指标一致性验证。
- [ ] 报告目标检测 mAP@0.5 和 mAP@0.5:0.95。
- [ ] 报告车道与可行驶区域的 mIoU、Precision、Recall 和 F1。
- [ ] 输出按场景分桶的结果，包括白天、夜间、拥堵、雨雾、阴影和车道磨损。
- [ ] 保存并展示置信度最高的错误检测和最严重的漏检案例。

### 4.2 时序跟踪与场景状态

- [ ] 在 YOLOPv2 检测结果后接入 ByteTrack，生成稳定的 track ID。
- [ ] 向道路上下文增加在视目标数、进入/离开事件和轨迹持续时间。
- [ ] 对跟踪结果报告 IDF1、ID Switch 和 Track Retention。
- [ ] 对检测短暂丢失、遮挡重现和拥堵场景进行专项测试。

### 4.3 性能优化

- [ ] 建立统一 benchmark，分别测量预处理、模型推理、后处理和完整帧延迟。
- [ ] 对比 512 与 640 输入尺寸的精度—速度权衡。
- [ ] 对比 CPUExecutionProvider 与 CoreMLExecutionProvider。
- [ ] 报告冷启动、预热后 p50、p95、FPS、内存占用和模型加载时间。
- [ ] 检查线程数、图优化和静态输入对性能的影响。
- [ ] 对连续视频进行至少 60 分钟稳定性测试。

### 达到程度

- [ ] ONNX 模型相对参考实现的检测/分割指标下降不超过 2 个百分点。
- [ ] Apple Silicon 720p 统一道路感知目标达到 15 FPS 以上；若未达到，必须给出瓶颈分析和优化前后对比。
- [ ] CoreML 相对 CPU 的端到端速度提升目标不低于 30%。
- [ ] 稳定性测试期间无崩溃、无持续内存增长、无输出尺寸漂移。
- [ ] 所有性能结果注明硬件、系统、Python、依赖版本、输入尺寸、预热次数和测量次数。

## 5. 统一上下文、事件与运行时

### 需要完成

- [ ] 为 Driver、Road、Vehicle 上下文定义明确版本号和 schema。
- [ ] 所有感知结果携带时间戳、序列号、来源、置信度、有效性和处理延迟。
- [ ] 严格区分 `False`、`None/UNKNOWN` 和 stale context。
- [ ] 对上下文更新增加类型、范围和枚举校验。
- [ ] 使多字段上下文更新具备原子性，失败时不留下半更新状态。
- [ ] 为驾驶员风险、车道丢失和可行驶区丢失增加时间去抖与事件冷却。
- [ ] 将摄像头采集、推理、上下文更新和展示拆成有界队列。
- [ ] 支持舱内、道路和 Agent 使用不同运行频率。
- [ ] 队列满时使用明确的丢帧策略，避免无限积压旧帧。
- [ ] 增加运行时健康状态、组件心跳、最近错误和延迟统计。
- [ ] 将事件写入结构化 trace，支持离线重放和问题定位。

### 达到程度

- [ ] 运行时能够识别并报告 stale、missing 和 invalid 数据，而不是把它们解释为正常状态。
- [ ] 感知到事件发布的 p95 延迟有自动化统计。
- [ ] 60 分钟运行期间上下文 freshness 达标率达到 99% 以上。
- [ ] 高频输入下内存有界，不发生队列无限增长。
- [ ] 同一 trace 重放时产生一致的上下文变化和事件序列。

## 6. Agent 执行框架

### 6.1 确定性安全边界

- [ ] 在 ToolRegistry 执行层强制检查 `requires_confirmation`，不能只依赖 Prompt。
- [ ] 未获得有效确认时，任何导航或敏感动作必须返回 `CONFIRMATION_REQUIRED`。
- [ ] 用户确认后只能执行 PendingAction 中保存的原始 tool name 和规范化参数。
- [ ] 用户拒绝、修改目标或确认超时后必须清除 PendingAction。
- [ ] 为确认 token/action ID 增加一次性使用和过期机制。
- [ ] 工具执行失败、参数错误和未知工具均返回稳定的结构化错误。
- [ ] 为工具定义读操作、可逆写操作和敏感动作三个风险等级。
- [ ] 将安全策略实现为确定性代码，不允许 LLM 覆盖。

### 6.2 上下文与事件使用

- [ ] Context Selector 同时考虑用户意图、数据 freshness 和置信度。
- [ ] Agent 回答中不得把 stale/UNKNOWN 感知结果陈述为确定事实。
- [ ] 将高风险驾驶员事件接入 Agent，但采用频率限制避免重复打扰。
- [ ] 为主动提醒定义触发条件、冷却时间、确认状态和取消条件。
- [ ] 将每次模型请求、选中上下文、工具调用、工具结果和最终回复保存为可脱敏 trace。
- [ ] 支持完全离线的 FakeLLM，用于确定性单元测试和回归测试。

### 6.3 模型与成本

- [ ] 保持 Qwen/GLM provider 接口一致。
- [ ] 固定评测温度、模型版本、重试策略和最大工具轮次。
- [ ] 统计每个任务的输入/输出 token、模型延迟、工具延迟和估算成本。
- [ ] 对 API 超时、限流、空回复、非法 JSON 和重复工具调用实现降级策略。

### 达到程度

- [ ] 敏感动作确认合规率达到 100%。
- [ ] 自动评测中未确认敏感动作执行次数为 0。
- [ ] Prompt Injection 不得绕过 ToolRegistry 安全检查。
- [ ] 同一 PendingAction 不得被重复执行。
- [ ] LLM 不可用时，系统仍能完成感知、上下文更新、事件记录和安全告警。

## 7. Agent 自动化评测与消融

### 需要完成

- [ ] 建立不少于 120 个评测任务。
- [ ] 每个任务包含输入上下文、用户消息、期望工具、期望参数、期望状态变化和禁止行为。
- [ ] 覆盖状态问答、单工具、多工具、确认、拒绝、过期上下文、工具失败和 Prompt Injection。
- [ ] 每个非确定性任务至少运行 3 次，并保存完整 trial trace。
- [ ] 实现确定性 grader：工具选择、参数匹配、状态变化和禁止动作。
- [ ] 实现语义 grader：回答是否基于上下文、是否正确表达不确定性、是否虚构执行结果。
- [ ] 对比无上下文与完整上下文。
- [ ] 对比全量上下文与 Context Selector。
- [ ] 对比 Prompt-only confirmation 与确定性执行门。
- [ ] 对比无 PendingAction 与 grounded PendingAction。
- [ ] 在相同评测集上比较 Qwen 与 GLM，或两个明确版本的模型。

### 达到程度

- [ ] 综合 Task Success Rate 目标不低于 90%。
- [ ] Tool Selection Accuracy 目标不低于 95%。
- [ ] Tool Argument Exact Match 目标不低于 95%。
- [ ] Grounded Answer Accuracy 目标不低于 95%。
- [ ] 未确认敏感动作执行率必须为 0%。
- [ ] 报告平均值、失败次数、波动范围、token、延迟和成本，而非只展示成功对话。
- [ ] 至少给出 10 个失败案例的根因分类和修复前后结果。

## 8. CARLA 仿真与场景契约

### 需要完成

- [ ] 接入 CARLA RGB Camera、ego pose、速度、道路和交通参与者真值。
- [ ] 明确相机坐标系、车辆坐标系和世界坐标系之间的转换。
- [ ] 对传感器帧进行时间同步，并记录仿真时间戳和 frame ID。
- [ ] 定义独立于 CARLA API 的 `SceneSnapshot`，避免 Agent 直接依赖仿真器对象。
- [ ] SceneSnapshot 至少包含 ego state、动态目标、车道状态、交通信号和数据有效性。
- [ ] 将 CARLA 真值作为道路感知与跟踪模块的评测参考。
- [ ] 建立正常道路、拥堵、行人横穿、车道线缺失和夜间五类可复现场景。
- [ ] 每个关键场景使用固定 seed 运行至少 10 次。
- [ ] 将仿真场景转换成可离线重放的 trace。
- [ ] Agent 只执行高层模拟动作，不连接真实车辆控制器。

### 达到程度

- [ ] 同一 seed 和配置能够复现实验场景与关键事件。
- [ ] 感知输出可与 CARLA 真值自动对齐并生成误差报告。
- [ ] 至少三个场景完成“感知 → 上下文 → 事件 → Agent → 受控动作”闭环。
- [ ] 关键端到端场景 10 次重复运行的成功率目标不低于 90%。
- [ ] 明确说明仿真结果与真实道路部署之间的 domain gap。

## 9. 端到端演示场景

### 需要完成

- [ ] 场景一：持续闭眼/疲劳 → 时序模型判定风险 → 高风险事件 → Agent 建议休息。
- [ ] 场景二：用户同意建议 → 搜索休息区 → 保存规范 POI → 二次确认 → 启动模拟导航。
- [ ] 场景三：用户拒绝或修改目的地 → 原 PendingAction 取消，不执行旧动作。
- [ ] 场景四：摄像头短暂失效 → 状态进入 UNKNOWN/stale → Agent 正确表达无法确认。
- [ ] 场景五：道路拥堵或车道感知丢失 → 事件去抖 → Agent 给出受约束提示。
- [ ] 场景六：Prompt Injection 要求绕过确认 → 执行层拒绝并记录安全事件。
- [ ] 每个场景提供自动化回放输入、预期结果和回归测试。

### 达到程度

- [ ] Demo 可以在没有现场摄像头的情况下稳定复现。
- [ ] 每个场景都能输出完整 trace 和关键指标。
- [ ] 视频演示不只展示 UI，还展示输入、感知结果、上下文、事件、工具调用和执行结果。
- [ ] 演示中的每个数字都能在结果文件中找到对应来源。

## 10. 测试与质量保障

### 需要完成

- [ ] 补齐 fatigue、presence、head pose、context、selector、event、tools 和 agent 单元测试。
- [ ] 对所有阈值边界、空输入、非法输入、UNKNOWN 和 stale 状态编写测试。
- [ ] 使用 FakeLLM 覆盖单轮、多轮、连续工具调用和超轮次限制。
- [ ] 为 ContextManager 并发更新和 EventBus 并发发布增加测试。
- [ ] 为 ONNX 模型输出 shape、dtype、类别映射和 mask 尺寸增加契约测试。
- [ ] 为配置和模型缺失提供清晰错误信息测试。
- [ ] 增加离线集成测试和端到端 trace replay 测试。
- [ ] 增加静态检查、格式检查和类型检查。

### 达到程度

- [ ] 核心业务逻辑测试覆盖率达到 80% 以上。
- [ ] 安全策略、确认机制和工具执行路径覆盖率达到 95% 以上。
- [ ] 测试不依赖在线 API、摄像头或随机 LLM 输出。
- [ ] 所有已修复缺陷均有对应回归测试。

## 11. 可观测性与结果管理

### 需要完成

- [ ] 统一结构化日志格式，并为每次运行生成 run ID。
- [ ] 保存 Git commit、配置、模型哈希、数据 split、硬件和依赖版本。
- [ ] 记录感知延迟、队列长度、丢帧数、上下文 freshness、事件数和 Agent 调用信息。
- [ ] 建立机器可读的 `results.json` 和面向阅读的 Markdown 报告。
- [ ] 自动生成精度表、混淆矩阵、PR 曲线、延迟分布和消融图表。
- [ ] 对日志和 Agent trace 进行密钥及隐私信息脱敏。

### 达到程度

- [ ] 任意图表和简历数字可以从保存的结果文件重新生成。
- [ ] 不允许手工复制一个最优数字而缺少对应实验配置。
- [ ] 失败运行与成功运行使用同一套记录协议。

## 12. GitHub 与作品集呈现

### 需要完成

- [ ] 重写 README 首屏，使招聘者在 30 秒内看到项目目标、架构、核心指标和 Demo。
- [ ] README 明确区分自研模块、修改模块、预训练模型和第三方依赖。
- [ ] 增加完整架构图和感知到动作的数据流图。
- [ ] 增加感知指标、Agent 指标、部署指标和消融结果表。
- [ ] 增加成功案例、失败案例和已知限制。
- [ ] 增加 Quick Start、离线评测、Agent eval、CARLA 场景和 benchmark 命令。
- [ ] 制作 60–90 秒主演示视频。
- [ ] 制作一段展示失败恢复和安全确认的短视频。
- [ ] 发布技术报告，解释问题定义、系统设计、实验协议、结果和局限性。
- [ ] 发布 `v1.0.0` GitHub Release，并附模型/配置/结果摘要。
- [ ] 后续大型视频和模型放入 Release 或对象存储，不继续扩大 Git 历史。
- [ ] 准备一页项目面试说明，覆盖架构选择、指标、失败案例、优化过程和个人贡献。

### 达到程度

- [ ] README 首屏包含至少一项感知指标、一项 Agent 指标和一项性能指标。
- [ ] 招聘者无需本地运行即可理解项目贡献和可信证据。
- [ ] 招聘者按 Quick Start 可以复现实验或最小 Demo。
- [ ] 项目描述不使用“量产级”“自动驾驶系统”等无法证明的表述。

## 13. 简历数据准备

- [ ] 记录时序驾驶员状态模型相对规则基线的 Macro-F1 提升。
- [ ] 记录 ONNX/CoreML 相对 CPU 的速度提升和 p95 延迟。
- [ ] 记录 Agent 评测任务数、总 trial 数、任务成功率和安全合规率。
- [ ] 记录自动化测试数、覆盖率和端到端场景成功率。
- [ ] 为每条简历描述保存对应的结果文件或报告链接。
- [ ] 只有在指标复现通过后，才把数字写入简历。

## 14. 明确不做或后置的内容

- [ ] 不把简单接入预训练模型描述为自主提出的感知算法。
- [ ] 不在没有评测集的情况下宣称 Agent “准确”或“可靠”。
- [ ] 不在安全确认门完成前连接真实车辆控制接口。
- [ ] 不优先开发完整自动驾驶规划器；感知与 Agent 证据闭环完成后再评估。
- [ ] 不为了展示技术数量同时引入 ROS2、完整 SLAM、LiDAR 融合、VLA 训练和真实 CAN 控制。
- [ ] 不只保留成功 Demo；失败案例和限制必须进入最终报告。

## 15. 可选增强项

- [ ] 将道路感知部署到 NVIDIA GPU，并增加 TensorRT FP16/INT8 对比。
- [ ] 增加单目深度或 BEV/occupancy 表示，并在独立数据集上评测。
- [ ] 增加多摄像头或 LiDAR 融合，但必须具备标定、同步和独立指标。
- [ ] 将运行时封装为 ROS2 nodes，并测量消息延迟与丢帧率。
- [ ] 增加简单局部规划，仅在 SceneSnapshot 和 CARLA 评测稳定后进行。
- [ ] 增加英文技术报告或博客，展示一次完整的失败分析与改进过程。
