# VehicleMind 简历展示项目任务清单

> 项目定位：构建一个可复现、可解释的“舱内/舱外感知 → 统一上下文 → 安全事件 → Agent 编排 → 受控车机工具”系统，用于证明感知工程、Agent 可靠性和端到端系统能力。
>
> 执行原则：完整离线 Demo 和 Agent 内部重复评测已形成；当前已完成招聘展示闭环 A6，并推送待合并分支。优先展示业务链路、实现边界、技术栈与可信证据。完成任务并通过验收条件后，才将 `[ ]` 改为 `[x]`；每个简历数字必须能追溯到配置、trace 或结果文件。视频暂缓，不作为当前展示闭环的前置条件。
>
> 2026-09-26 更新：A1–A5 已有实现与各自证据；A6 前后回归、子集指标和招聘展示文档已完成。疲劳休息、正常音乐、取消/恢复三类离线案例均可独立生成中文报告。A6 使用每模型 40 条冻结合成场景 × 3 次，逐 trial 哈希校验并由 Qwen v4 辅助语义复核。历史预算字段缺失，因此前后差值为 N/A。舱内外无人工真值、道路类别语义映射和版本 Release 仍单列为后置，不将限制误勾为完成。

## 0. 最终完成标准

### 当前 Agent 深化主线（2026-09-25 方案，持续核验）

按 [Agent 深化方案](docs/superpowers/specs/2026-09-25-agent-evolution-roadmap.md) 推进，以下项目是新增目标，不代表已有能力。评测与中文展示贯穿每一阶段，既有历史条目保留供追溯。

- [x] 可靠上下文与任务状态：统一事实来源/时间/质量，补充任务相关风险证据，完善超时、空回复、重复调用和工具失败处理。已完成代码与验收；在线语义仍有已记录失败，详见 [A1 验收记录](docs/reports/2026-09-25-agent-a1-completion.md)。
- [x] 状态相关建议与执行策略：区分读取/可逆/需确认操作，完善风险建议、确认/取消及带冷却的事件主动触发。
- [x] 状态相关 RAG：建立有来源与适用范围的中文知识库，完成检索、引用与缺失反馈，在报告中展示实际知识依据；A3 在线证据见[报告](docs/reports/2026-09-26-a3-lightrag-evaluation.md)。
- [x] 行程内事件记忆：SQLite 持久化风险、提醒、用户请求、确认/取消与动作结果；按行程/类型/时间查询，Agent 使用只读工具；验收见 [A4 行程事件记忆报告](docs/reports/2026-09-26-a4-trip-event-memory.md)。
- [x] 受限计划与恢复：完成多候选地点、分步执行、失败替代与停止，控制总步数和重复动作；A5 七场景通过，见[验收报告](docs/reports/2026-09-26-a5-bounded-plan-recovery.md)。
- [x] 贯穿式评测与展示：三个独立离线主案例已发布并可复现；A5 后两模型在线全量回归、同协议 v4 复核、分项报告与招聘展示已完成。历史预算不完整，优化前后只作描述性并列、差值 N/A。详见 [A6 报告](docs/reports/2026-09-26-a6-online-regression.md)。

#### A1. 可靠上下文与任务状态

进度（2026-09-25）：A1 已完成。决策上下文、任务状态、预算/异常控制、确认与结果核对、在线 trial 和离线 replay trace、中文 HTML 展示均已实现。确定性测试和在线定向评测记录见 [A1 验收记录](docs/reports/2026-09-25-agent-a1-completion.md)。在线语义验收仍存在明确失败，已标为未解决并保留原始回答；A1 完成不代表模型语义全部正确或产生总体成功率结论。

**技术栈**：沿用 Python 3.13、dataclasses/Enum、ContextManager、ContextSelector、ObservationMetadata、Qwen/GLM 客户端和 JSON Schema 工具协议；新增 `AgentTask` 任务状态、YAML 调用预算、参数验证、只读状态核对及独立 turn/session 模块。在线记录用 JSON，回放沿用现有语义 trace。

**需要做什么**：

- [x] 将观测、用户自述、工具结果和历史信息整理为带来源/时间/质量的决策输入；保留实际存在的置信度，不自动补造。
- [x] 完善现有关键词路由与任务关联：音乐、休息和导航请求能够带入相关风险信息；多轮“就去这个”可关联当前目标或在没有有效目标时拒绝盲目执行。
- [x] 对齐状态枚举与实际算法含义；提示约束禁止把用户自述改写为检测结果、把轻车流改写为拥堵、把过期值改写为当前事实；在线仍有语义失败，保留失败证据。
- [x] 保存当前目标、最近及完整工具结果和待确认动作，明确等待输入、执行中、待确认、完成、失败与取消状态。
- [x] 统一超时、空回复、参数错误与工具异常；使用 `modules/config/agent.yaml` 限制单轮时间/轮数/工具次数、重复调用及 trace 长度；写操作结果未知时优先调用相应只读状态工具。

**达到什么结果**：

- [x] 正常、高风险、观测过期三个主场景均能显示本轮实际送入模型的事实与来源，并记录字段质量。
- [x] C03/R03/R06/X05 等已知语义失败有针对性回归记录；正确处理与未解决失败分别报告，注明 AI 自审及小样本边界。
- [x] 超时/空回复/错误参数/重复调用四类异常均能有限结束，并保存明确结果；报告中可区分“待确认”和“执行失败”。
- [x] 多轮用户变更目标或取消后，后续动作与当前任务一致；每次动作均可从在线 trace/replay trace 追溯。

#### A2. 状态相关建议与执行策略

**技术栈**：沿用 EventDetector、EventBus、ToolRegistry、PendingAction 和显式确认入口；新增基于 Python Enum/dataclass 的工具风险等级、确定性策略判断器、YAML 策略配置和带冷却的事件触发器。LLM 继续通过 Qwen/GLM Function Calling 给出回复与工具请求。

**需要做什么**：

- [x] 为现有工具定义读取、可逆写入、需确认操作三类，结合观测质量、车辆状态和用户意图给出允许/待确认/拒绝结果。
- [x] 为正常、风险升高、高风险、数据不可用状态配置建议约束；高风险疲劳建议停车休息，播放音乐不被表述为消除疲劳。
- [x] 统一用户确认、拒绝、改目的地和超时的任务转换，保持规范 POI 标识与一次性授权。
- [x] 将适用的风险事件接入低频 Agent 建议入口，配置去重和冷却；记录触发事件及实际模型调用。

**达到什么结果**：

- [x] 同一句“放点音乐”在正常、高风险疲劳和状态未知时，能展示不同且有依据的处理结果。
- [x] 持续输入同一风险时，模型调用次数符合冷却规则；风险事件可触发建议，工具写入仍能追溯用户授权。
- [x] 确认、取消、过期与重复确认的确定性验收场景中，未经确认的敏感动作执行数为 0。
- [x] 报告展示建议约束、实际模型建议和执行策略结果，并统计 Recommendation Appropriateness（建议适配率）与 Confirmation Compliance（确认合规率）。

#### A3. 状态相关 RAG

**技术栈**：LightRAG 固定版本 REST sidecar（与 Python 3.13 主环境隔离，按 `vehicle_common`、`vehiclemind_demo` 运行独立固定端口/目录/索引）；LightRAG `mix` + context-only 查询提供证据，不生成最终答复。VehicleMind 使用白名单 profile-to-endpoint 路由、只读 Agent 工具与 Qwen/GLM Function Calling；知识源用带 SHA-256、来源、章节、版本和适用范围的 YAML/Markdown 清单管理。按验证后锁定的 LightRAG 与 embedding 配置运行，不把 sidecar 依赖加入主环境。

**需要做什么**：

- [x] 建立首批 20 条可公开、可追溯知识条目；通用资料 10 条在两个固定索引中复用同一 ID/哈希，VehicleMind 项目资料 10 条只进入 `vehiclemind_demo`。
- [x] 固定并实测 LightRAG 1.5.7 REST/API 与 `text-embedding-v3`；双 profile 独立目录和端口，暂存完整索引后发布，失败不替换既有索引。
- [x] 以白名单 profile 路由调用 LightRAG `mix` context-only；保存前五条引用与片段。只注入与问题相关且质量为 `KNOWN` 的上下文。
- [x] 建立只读 `search_vehicle_knowledge` 工具，Agent 按需调用；检索错误或无命中明确回传；知识引用不创建 PendingAction、不改变工具权限。
- [x] 在工具结果与报告中分开静态知识和当前实时状态；当前评测未注入实时状态时明确显示“本题未注入已验证的实时车况”。

**达到什么结果**：

- [x] 实测“困倦线索 / 休息建议”和 VehicleMind 舱内证据两个在线问题，Agent 主动检索并返回来源 ID、章节、片段及来源链接。
- [x] 冻结 30 条 AI 辅助内部问题：20 条可回答、5 条无答案、5 条 profile 不匹配；明确不是独立人工金标。
- [x] 生成真实在线指标与逐题中文 HTML 报告：Recall@5 20/20；Citation Support 69/74；无答案 AI 审查 5/5；检索来源范围泄漏 0。首轮范围弃答为 2/5。
- [x] 发现参数记忆绕过检索后，强化项目实现问题检索约束；同一 5 条范围题独立复测为 5/5 AI 审查通过。首轮基线与修正后定向复测分开记录，不混算。
- [x] 在线运行保存题集、知识清单、双索引 manifest、模型版本和哈希；索引可从原始文本重新构建。

进度（2026-09-26）：A3 LightRAG 检索、只读 Agent 工具、20 条来源语料、双 profile 实际索引、30 题在线基线、AI 辅助引用/弃答审查及中文报告均已完成并通过验证。完整数据和限制见 [A3 在线评测报告](docs/reports/2026-09-26-a3-lightrag-evaluation.md)。当前主要观察项：基线 profile 范围拒答 2/5，提示修正后的 5 题复测为 5/5；需要后续完整重跑观察新提示在全量题上的表现。

技术参考与已确认设计：[A3 LightRAG 检索器设计](docs/superpowers/specs/2026-09-25-a3-lightrag-design.md)。LightRAG 只提供检索证据；最终答复与安全/执行策略仍由 VehicleMind Agent 和策略层负责。

#### A4. 行程内事件记忆

进度（2026-09-26）：A4 已完成。SQLite 行程事件表、全局唯一事件 ID 去重、30 天保留、按 trip/type/time 查询与清空已实现；运行时记录语义风险、主动提醒、用户请求、确认/取消和工具结果；Agent 的历史查询工具为只读且最多返回 50 条。独立视频 Demo 支持固定行程 ID 重启续读、`history` 查看和确认后清空。冻结结构化检索查询 4/4，8/8 总数与事件 ID 字段匹配；旧 HIGH 风险未注入当前车况，时间混淆注入检查 0/1。限制与复现方式见 [A4 验收报告](docs/reports/2026-09-26-a4-trip-event-memory.md) 和 [Demo 指南](docs/guide/trip-memory-demo.md)。

**技术栈**：新增 Python 标准库 `sqlite3` 本地存储、SQL 时间范围查询、唯一事件 ID 与去重约束；沿用 EventBus、任务状态和工具轨迹。当前状态仍由 ContextManager 管理，对话由当前会话管理，知识由 A3 检索。

**需要做什么**：

- [x] 定义行程/会话标识和事件表，记录风险、提醒、用户选择、确认/取消、动作结果、时间和来源。
- [x] 按行程、事件类型与时间范围读取历史，支持去重、保留期限与清空。
- [x] 为 Agent 增加受限的历史查询接口，返回带时间的事实；历史记录和当前车况明确隔离。

**达到什么结果**：

- [x] Agent 可按需查询“这次行程提醒过几次”“刚才取消了哪个操作”，结果保留事件 ID、时间、来源和结构化证据。
- [x] 固定事件序列中计数、去重、跨行程隔离和时间排序通过；旧疲劳事件不会覆盖当前正常状态。
- [x] 报告 Event Retrieval Accuracy（历史事件检索正确率）8/8 字段匹配（4/4 查询），Temporal Confusion（时间混淆次数）注入检查 0/1；重启可续读、清空后无法召回。

#### A5. 受限计划与失败恢复

进度（2026-09-26）：A5 已实现并通过七个确定性故障场景。受限计划最多 9 步、最多 1 次恢复；模拟 POI 过滤、候选 ID 约束、一次只读重试、一次替代候选且必须新确认、无结果/取消/未知写入安全停止均可复现。步骤会进入 Agent trace，并在启用 A4 SQLite 时以 `TASK_STEP` 保存。运行入口、逐场景证据与限制见[验收报告](docs/reports/2026-09-26-a5-bounded-plan-recovery.md)和[运行指南](docs/guide/agent-plan-recovery.md)。指标只代表确定性软件验收，不代表在线模型成功率或实车安全。

**技术栈**：沿用 VehicleAgent、ToolRegistry、PendingAction、Qwen/GLM Function Calling 和 A4 SQLite；新增 Python dataclass/Enum 状态机、YAML 配置（最多 9 步/1 次恢复/3 个候选）、确定性 POI 目录与脚本化故障注入。未引入新的 Agent 框架。

**需要做什么**：

- [x] 为休息地点任务建立有上限的步骤：理解需求、查询知识/地点、选择候选、等待确认、执行与结果核对。
- [x] 扩展模拟 POI 为多个可筛选候选，支持无结果与主候选失败场景；报告明确其模拟属性。
- [x] 对可重试的只读查询最多重试一次；非候选 POI 直接拒绝；未知写入结果只读核对并停止，不自动重放。
- [x] 每一步保存状态、成功条件、证据/失败原因及剩余预算；支持用户取消和目标变更。

**达到什么结果**：

- [x] 展示一次成功导航、一次主地点失败后选择替代地点、一次无可行结果后明确停止；另覆盖取消、非法目标、未知写入和只读重试。
- [x] 七个确定性场景均不超过 9 步/1 次恢复预算；取消后无写操作，未知结果只尝试一次写入并核对状态。
- [x] 报告展示实际步骤及恢复原因：Recovery Success（恢复成功率）1/1、Safe Stop（安全停止）4/4、Duplicate Write（重复写操作违规）0/7、Budget Compliance（预算合规率）7/7。

#### A6. 贯穿式评测与招聘展示

**技术栈**：沿用 pytest、版本化 YAML 场景、确定性 grader、Qwen/GLM 在线重复运行、AI 辅助语义审核、JSON trace、中文 HTML/CSS 报告、Ruff/mypy 和 GitHub Actions；新增 A1–A5 专项指标与可追溯汇总页。展示继续使用英文指标名加中文含义。

**需要做什么**：

- [x] 将旧 40 场景作为已审阅的历史回归集；A5 新能力另有版本化确定性场景、成功标准和证据引用，不再把旧 heldout 当作未见测试集。
- [x] 冻结当前 rubric 与语义审核协议 v4，Qwen/GLM 使用 40 条场景各重复 3 次；旧 trace 以隔离副本重评，新旧逐 trial 失败和异常保留。历史运行缺少部分预算，已报告为不可归因的并列结果。
- [x] 汇总上下文依据、工具/参数/状态、语义通过、延迟、token、请求数与可核验费用；A2、A3、A4、A5 使用自己的分母独立引用，不合成总分。过期/未知及逐次波动以固定场景子集补充展示。
- [x] 更新在线决策 HTML：舱内外 GIF 与关键事实、实际模型输入摘要、知识引用、A5 任务步骤/候选/预算/恢复、确认与动作结果；原始 JSON 折叠。
- [x] 更新 README 与面试讲述卡，为疲劳休息、正常音乐、取消/异常恢复主案例给出启动命令、结果入口、技术栈、个人实现与已知失败。

**达到什么结果**：

- [x] 疲劳休息、正常音乐、异常恢复三个主案例均可自行启动，并生成对应中文报告；运行和浏览器报告通过回归测试。
- [x] 每个 A6 对外数字可追溯到模型、代码版本、冻结场景和本机原始结果哈希，并明确分子分母；重复次数不增加独立样本量。
- [x] 发布同协议 v4 的前后描述性报告，保留退化、失败、AI 自审边界与历史预算缺项；差值为 N/A，不做消融实验。
- [x] A6 任务状态已更新；代码及公开报告提交到功能分支并推送 GitHub。带版本号 Release 继续列作独立后置任务。

### 总体验收清单

- [x] 全新 clone 后按 README 完成开发依赖安装、离线 Demo、全量离线测试和结果查看；干净克隆 625 passed、2 deselected，疲劳场景 9/9 断言通过、未授权敏感执行 0。
- [x] 离线 Demo 不依赖摄像头、模型下载、API Key 或在线 LLM；依赖安装完成后可离线运行，真实视频推理另列可选项。
- [x] 疲劳回放展示舱内/舱外结果、统一上下文、事件、Agent 决策摘要、确认状态、工具调用和执行结果；音乐案例补足非导航可逆动作链路。
- [x] 无标签感知核验按舱内/舱外各不超过 50 条执行；本次各 26 条视频只报告处理覆盖和输出分布，明确不等同准确率评测。
- [x] 建立 40 条 AI 自审内部评测场景与 80 条开发回归变体；Qwen/GLM 各在 40 条冻结场景上重复 3 次，输出注明局限的可靠性报告，不将 80 条变体计为独立金标。
- [x] 未经确认的敏感动作执行次数为 0；4 条离线提示注入场景均被执行层确认门拦截，0/4 未确认敏感写入。范围与限制见[专项核验](docs/reports/2026-09-26-agent-prompt-injection-safety.md)。
- [x] 核心业务范围测试覆盖率至少 80%（92.7%）；选定安全执行路径至少 95%（96.4%），按最终全量离线套件计算，见第 7 节。
- [x] README 与报告对关键结论给出成功/失败案例、数据类型、局限和相关硬件环境；不把模拟/录制观测当成实车或识别精度。
- [ ] 发布带版本号的 GitHub Release，包含可公开的结果摘要、复现说明和许可证边界；视频按先前决定暂缓。

## 1. 仓库与工程基线

### 需要完成

- [x] 建立功能分支与 Pull Request 工作流，禁止继续把未验证功能直接堆叠到 `main`。
- [x] 为 `main` 启用分支保护，要求 CI 通过后才能合并。
- [x] 补全 Python 依赖声明，并生成可锁定版本的环境文件。
- [x] 明确支持的 Python、macOS、Apple Silicon 和 CPU fallback 版本范围。
- [x] 配置 pytest 只收集 `tests/`，避免把 Demo 文件误识别为测试。
- [x] 修复 `face_present`/`presence` 等已经失效的 Demo 接口。
- [x] 将 `eye_closed` 统一为 `bool | None`，明确 `None` 表示当前没有可靠眼睛观测。
- [x] 删除、实现或解释仓库中的空模块、空测试和空配置文件。
- [x] 将硬编码阈值迁移至可版本化配置文件，并保存每次实验使用的配置。
- [x] 增加 `.env.example`，确保密钥和本地路径不进入 Git。
- [x] 增加模型资产清单，记录文件名、来源、版本、SHA-256、许可证和预期路径。
- [x] 增加 `LICENSE`、`THIRD_PARTY_NOTICES.md` 和贡献说明。
- [x] 建立 GitHub Actions，执行格式检查、静态检查、单元测试和核心离线 smoke test。

### 达到程度

- [x] 全新 clone 后不依赖开发者本机隐式环境即可安装。
- [x] `pytest` 能正常完成收集并全部通过。
- [x] 不连接摄像头、不下载大模型、不调用在线 API 时，核心逻辑测试仍可运行。
- [x] CI 在每个 Pull Request 上自动执行，失败时不能合并。
- [x] README 中的命令与真实入口一致，不存在已失效示例。

## 2. 完整离线 Demo

### 2.1 场景协议与回放

- [x] 定义带版本号的 YAML 场景格式，覆盖媒体、舱内观测、道路观测、车辆状态、用户输入、确认动作和预期结果。
- [x] 场景观测携带时间、来源、置信度和有效性，非法字段或路径在执行前失败。
- [x] 提供“疲劳驾驶 → 建议休息 → 搜索休息区 → 用户确认 → 启动模拟导航”的完整示例。
- [x] 实现确定性离线 Agent，保证 CI 中不调用在线模型。
- [x] 实现场景回放运行器，使同一场景产生一致的语义结果摘要。
- [x] 保存上下文变化、事件、Agent 回复摘要、待确认动作、确认、工具请求和工具结果。

### 2.2 结果展示

- [x] 提供同步的舱内/舱外媒体面板和感知状态叠加信息。
- [x] 展示 Driver、Road、Vehicle 上下文的来源、置信度、有效性和 freshness。
- [x] 展示风险事件时间线、Agent 决策摘要、确认状态、工具调用和执行结果。
- [x] 展示各阶段延迟、组件健康状态和场景断言结果。
- [x] 每次运行生成 `resolved_config.yaml`、`run_card.md`、`summary.json`、`trace.json` 和 `report.html`。
- [x] 页面不展示隐藏思维链，不把录制观测描述为实时模型推理结果。

### 达到程度

- [x] 一条命令可运行完整离线场景并生成可查看结果页。
- [x] Demo 在无摄像头、无模型权重、无 API Key、无网络环境下稳定通过。
- [x] 示例场景的所有预期事件、工具调用和最终车辆状态均由自动断言验证。
- [x] 运行产物包含 Git commit、配置哈希、场景版本、模型资产信息和语义 trace 哈希。
- [x] 失败场景使用同一协议输出明确失败原因，不留下半成品结果目录。

## 3. 统一上下文、事件与运行时

### 需要完成

- [x] 为 Driver、Road、Vehicle 上下文定义稳定版本和字段契约。
- [x] 统一上下文接入的舱内与道路感知结果携带时间戳、序列号、来源、置信度、有效性和处理延迟。
- [x] 严格区分 `False`、`None/UNKNOWN`、invalid 和 stale 数据。
- [x] 对上下文更新增加类型、范围和枚举校验。
- [x] 使多字段上下文更新具备原子性，失败时不留下半更新状态。
- [x] 为驾驶员风险、车道丢失和可行驶区丢失增加时间去抖与事件冷却。
- [x] 将离线视频读取、推理、上下文更新和展示拆成有界队列；实时摄像头采集留待有采集条件时接入。
- [x] 支持舱内、道路和 Agent 使用不同运行频率。
- [x] 队列满时使用明确的丢帧策略，避免无限积压旧帧。
- [x] 增加组件心跳、最近错误、队列长度、丢帧数和延迟统计。

### 达到程度

- [x] 运行时能够识别并报告 stale、missing 和 invalid，而不是解释为正常状态。
- [x] 推理完成到事件发布的 p95 延迟可自动统计。
- [x] 高频输入下各队列容量有上界，不发生队列无限增长。
- [x] 同一语义 trace 重放时产生一致的上下文变化和事件序列。

## 4. Agent 安全执行框架

### 4.1 确定性安全边界

- [x] 在 ToolRegistry 执行层强制检查 `requires_confirmation`，不能只依赖 Prompt。
- [x] 未获得有效确认时，导航或其他敏感动作返回 `CONFIRMATION_REQUIRED`。
- [x] 用户确认后只能执行 PendingAction 保存的原始工具名和规范化参数。
- [x] 用户明确拒绝、修改目标或确认超时后清除 PendingAction；模糊说法留待 Agent 自动化评测统计。
- [x] 确认 action ID 一次性使用且具有过期机制，重放必须失败。
- [x] 工具执行失败、参数错误、未知工具和确认不匹配返回稳定结构化错误。
- [x] 工具定义读操作、可逆写操作和敏感动作三个风险等级，使用 `ActionRisk` 与 `modules/config/agent_policy.yaml` 显式配置。
- [x] 安全策略由确定性代码执行，LLM 无权覆盖。

### 4.2 上下文和异常处理

- [x] Context Selector 按用户意图选择主题、按 freshness/field quality 排除不可用事实，并把置信度作为逐字段来源证据送入上下文；未对未校准的 confidence 设任意阈值。
- [x] Agent 不得把 stale、invalid 或 UNKNOWN 感知结果陈述为确定事实；系统提示、字段筛选及相关回归测试覆盖该边界。
- [x] 高频风险事件使用冷却策略，避免重复打扰驾驶员；冷却配置与重复触发测试可核验。
- [x] 在线模型超时、模拟 provider 限流、空回复、非法工具参数和重复工具调用均有离线异常回归，并在预算内安全结束或拒绝工具请求；线上 provider 的真实 429 状态码尚未实测。
- [x] LLM 建议不可用时，舱内感知/上下文/风险事件不被中断，并记录不泄露异常正文的确定性安全停车建议；fallback 不调用任何车机工具，见事件推荐与行程记忆回归。

### 达到程度

- [x] A5 七个固定恢复场景和三条离线主案例中，预设的确认边界均通过，未确认敏感动作执行为 0；仅适用于这些有限模拟场景。
- [x] 建立 4 条用户/检索证据提示注入场景及执行层 grader；每条均要求 `CONFIRMATION_REQUIRED`，未确认敏感写入 0/4。范围仅覆盖 ToolRegistry / PendingAction，不代表模型的通用注入识别能力，见[专项核验](docs/reports/2026-09-26-agent-prompt-injection-safety.md)。
- [x] 同一 PendingAction 不得重复执行。

## 5. Agent 自动化评测

> 在线评估原计划要求 40 条独立人工审定；按用户 2026-09-24 新授权，现改为 40 条 Codex AI 自审的**内部**金标准与 80 条开发回归变体。两者不混计；候选、未完成语义复核的 trial 不计入端到端成功率，不得称独立人工评测。

- [x] 建立在线 Agent 单 trial 的离线可测运行器、确定性基础评分与中文候选报告。
- [x] 双模型真实 API 预检及 8 条开发集候选 pilot，保存单次轨迹和中文技术报告；不计正式成功率。
- [x] 修复 `CONFIRMATION_REQUIRED` 导致的重复工具调用和误导性失败回复，完成 Qwen/GLM 的 M01 单例复测与回答依据技术审查。
- [x] 为 8 条 pilot 候选场景建立事实来源、必要结论、允许/禁止推断 rubric，并实现模型身份隐藏的盲审材料与人工决定校验；仍待用户审定金标。
- [x] 补齐舱内组 C01–C06 六条候选场景和逐条回答依据 rubric；4 条开发集、2 条保留集候选均通过结构及离线运行校验，仍未人工审定或冻结。
- [x] 补充道路组 R01–R05 候选场景及逐条 rubric，覆盖车流、车道、可行驶区、行人与骑行者、交通设施；R05 仅是未冻结的保留集候选。
- [x] 在评测运行器中注入道路无效/过期观测并向 Agent 传递质量状态，补齐 R06 候选场景；仍待人工审定，不能当作冻结金标。
- [x] 按用户 2026-09-24 授权完成 40 条场景的 Codex AI 自审，冻结 24 条开发集和 16 条保留集，保存逐条说明与哈希；明确不是独立人工标注。
- [x] 从冻结开发集生成 80 条独立编号的候选回归变体，完成 10 条分层 AI 抽检；不把变体当作新增金标。
- [x] 两模型在冻结 40 条上各重复 3 次，并完成逐 trial 在线 AI 辅助语义复核与证据纠错，发布**内部** Task Success；`needs_review` 不计成功。结果与局限见 [中文报告](docs/reports/2026-09-24-online-agent-internal-evaluation.md)。

### 需要完成

- [x] 建立 120 条不同编号的 Agent 任务：40 条 AI 自审冻结内部场景、80 条仅用于开发回归的候选变体；后者不混入金标成绩。
- [x] 每个冻结任务记录场景输入上下文、用户消息、期望工具/规范参数、期望车机状态、必要事实与禁止表述。
- [x] 40 条冻结集和离线测试覆盖状态/上下文问答、读工具、可逆动作、敏感动作、确认/拒绝、超时、UNKNOWN/stale、非法参数与工具失败。
- [x] 为 Prompt Injection 建立专项 YAML 场景、自动 grader 和 ToolRegistry 确认门绕过回归；4/4 攻击尝试均未执行未确认写入。
- [x] 冻结集的非确定性在线模型任务每模型至少运行 3 次，并保存每次 trial trace。
- [x] 实现工具选择、参数匹配、状态变化和禁止动作的确定性 grader。
- [x] 实现在线 AI 辅助逐 trial 回答语义审核、原始决定留存与 Codex 证据纠错；**不是独立人工语义 grader**。
- [x] 固定本次评测的模型版本、温度、重试策略和最大工具轮次。
- [ ] 补齐工具调用级延迟与重试次数汇总，并核验两家模型当期单价；A6 已提供 per-trial token、端到端延迟、请求数与重复波动，Qwen 按公开费率估算，GLM 费率无法核验故成本为 N/A，见 [A6 报告](docs/reports/2026-09-26-a6-online-regression.md)。
- [x] 对已知在线语义失败和确认编排缺陷分类并保存修复前后证据；本轮新增参数编码、M01 指代、M05 过期问题均有根因说明与回归测试。

### 量化指标

- [x] 报告内部端到端 Task Success Rate，并注明 AI 自审边界。
- [x] 报告 Tool Selection Accuracy。
- [x] 报告规范化 Tool Argument Exact Match。
- [x] 报告 Context Grounding Correctness（上下文依据子集语义通过率）：Qwen 38/66（57.6%）、GLM 48/66（72.7%）；口径与 AI 辅助审核限制见 [A6 报告](docs/reports/2026-09-26-a6-online-regression.md)。
- [x] 报告 Confirmation Compliance Rate；A5 七场景执行层确认合规率 7/7，另有 4 条注入专项攻击 0/4 未确认敏感写入。
- [x] 报告 stale/UNKNOWN Handling（过期/未知子集语义通过率）：Qwen 4/18（22.2%）、GLM 9/18（50.0%）；口径与 AI 辅助审核限制见 [A6 报告](docs/reports/2026-09-26-a6-online-regression.md)。
- [x] 报告确定性安全策略的重放一致率；A5 固定场景集双次执行结果一致 7/7。
- [x] A5 与三个离线主案例均报告未确认敏感动作执行数 0（范围为固定模拟场景）。
- [x] 同时报告失败次数、重复 trial 波动、token、延迟和成本；A6 报告逐项给出当前数据，无法核验的 GLM 费用明确为 N/A，不伪造账单；前后变化仍因历史预算缺项而不可归因。

## 6. 小样本感知验证

> 零人工标注自动核验按 [实施计划](docs/superpowers/plans/2026-09-24-zero-label-perception-audit.md) 分步推进。现有视频无真值标签；下方涉及 Precision、Recall、F1、误报/漏报与事件延迟的条目不能由无标注视频完成。

- [x] 建立舱内/舱外各最多 50 条的确定性视频清单扫描，记录哈希、媒体元数据与 `unknown` 来源/许可/标签状态；损坏视频仍纳入尝试分母。
- [x] 完成逐视频模型服务适配器：完整逐帧、状态隔离、结构化抽样及失败继续；伪视频/伪服务测试通过，真实权重与视频运行待核验。
- [x] 增加自动核验命令、不可覆盖的本地运行产物、中文汇总报告及 `not_evaluated` 精度状态；伪服务集成测试和真实视频运行均已完成。
- [x] 使用真实权重完成舱内 26 条、舱外 26 条视频的逐帧自动核验；公开摘要只报告处理覆盖与输出分布，精度仍为 `not_evaluated`。
- [x] 在没有可用标签时，只把每侧 26 条作为处理覆盖/输出分布核验，并明确小样本限制；不把零标注运行包装成识别精度评测。
- [x] 静态审查舱外目标仅出现 `truck` 的类别映射、模型来源与后处理：发现检测头按 85 值/80 类解释、展示名表只有 10 类的契约疑点；权重哈希与资产清单一致，类别能力继续不作为展示证据。详见[静态审查](docs/reports/2026-09-26-road-class-mapping-audit.md)。
- [x] 在 ONNX Runtime 1.30.0 / CPUExecutionProvider 下记录动态、静态 512、静态 640 三个本地导出的 raw head shape 与 NMS class ID；同一 `road_test.mp4` 的首/中/末帧均只输出 ID 3，命令与 SHA-256 见[运行时审计](docs/reports/2026-09-26-road-class-mapping-audit.md)。
- [ ] 取得与本地导出权重对应的上游 checkpoint 和训练类别元数据，确认 ID 3 的真实语义，并在 UI/上下文中安全呈现；上游存在仍未解决的 class 3 讨论，当前 `truck` 映射不作为可信结论或简历指标。

### 6.1 舱内感知

- [ ] 建立舱内最多 50 条独立短视频的验证清单，记录来源、授权、哈希和标签方式。
- [ ] 标签至少覆盖 `NORMAL`、`DROWSY`、`DISTRACTED` 和 `UNKNOWN`，有条件时记录事件区间。用户已明确不做人工标注；没有可直接复用且获准的数据集标签前，准确率指标保持未评估。
- [ ] 验证驾驶员存在、闭眼/PERCLOS、哈欠、疲劳状态和手机分心输出。
- [ ] 报告每类样本数、Precision、Recall、F1、混淆矩阵和事件检测延迟。
- [ ] 展示典型成功、误报、漏报、遮挡和低质量画面案例。

### 6.2 舱外感知

- [ ] 建立舱外最多 50 条独立图像或短视频的验证清单，记录来源、授权、哈希和标签方式。
- [ ] 只标注 Demo 实际展示的目标框/类别、车道状态和可行驶区域。用户已明确不做人工标注；没有可直接复用且获准的官方标签前，准确率指标保持未评估。
- [ ] 对有标签的输出报告适合小样本的检测或分割指标，不外推为公开 Benchmark 结论。
- [ ] 检查类别映射、坐标缩放、letterbox、mask resize 和 NMS 的正确性。
- [ ] 展示典型成功、误检、漏检、夜间、阴影和车道磨损案例。

### 6.3 性能

- [x] 道路感知：分别测量预处理、模型推理、后处理和完整帧延迟；舱内另行报告服务初始化、首帧与稳态单帧延迟，不虚构内部阶段分解。
- [x] 道路感知：报告当前缓存状态下会话初始化、首帧、预热后 P50/P95、FPS 与进程峰值 RSS；舱内性能指标见[本机报告](docs/reports/2026-09-26-cabin-performance.md)。
- [x] 道路感知：对比 CPUExecutionProvider 与 CoreML 优先配置的实际 provider 和本机性能；不声称纯 CoreML。
- [x] 道路感知：注明硬件、系统、Python、依赖版本、输入尺寸、预热与测量次数；舱内记录 26 条视频的版本、模型/配置哈希及 `cv2` 实际运行时，另见[本机报告](docs/reports/2026-09-26-cabin-performance.md)。
- [x] 舱内感知：对 26 条视频 / 30,063 帧记录服务初始化、首帧、稳态 P50/P95 及离线回放吞吐；报告明确这不是准确率、实时摄像头 FPS 或 Agent 端到端延迟。

### 达到程度

- [x] 舱内结果明确声明本次处理 26 条（≤50）及统计局限；没有真值标签，准确率仍未评估。
- [x] 舱外结果明确声明本次处理 26 条（≤50）及统计局限；没有真值标签，准确率仍未评估。
- [x] 舱内外 26 条处理覆盖结果与道路性能结论可追溯到冻结样本 manifest、配置文件哈希、模型 SHA-256 和 Git commit；新增 ONNX 类别 ID 抽样报告同样记录视频/模型哈希、阈值及源代码版本。原始运行产物仍只保存在本机 `runs/`，公开仓库不能从这些原始记录完全再生成全部图表。
- [x] README 与感知说明区分第三方预训练权重和项目内适配/时序语义处理，不把接入写成新模型发明。

## 7. 测试、可观测性与结果管理

### 需要完成

- [x] 已有 fatigue、presence、context、selector、event、tools、safety 和 Agent 的分层单元测试；完整离线套件本轮通过。
- [x] 覆盖阈值边界、空/非法输入、UNKNOWN、invalid 和 stale；具体范围可见 `tests/`。
- [x] 确定性 Agent 覆盖单轮、多轮、连续工具调用、工具循环/时间/调用预算及超轮次限制。
- [x] 配置、场景、模拟工具错误有结构化错误路径测试；provider 缺密钥未在本轮调用在线服务实测。
- [x] 已有无密钥离线集成及端到端 trace replay smoke tests。
- [x] 使用安全唯一 `run_id` 并拒绝路径穿越；运行报告保存 Git 版本、配置快照、场景版本/哈希，感知运行记录模型资产哈希与硬件。
- [x] 完成 Git 跟踪文本与本机 `runs/` 下 1,110 个文本日志/trace/report 的路径及常见凭据模式扫描；未发现凭据模式，发现 6 个被 `.gitignore` 排除的 LightRAG 日志含本机绝对路径，未对本机日志作破坏性修改。扫描范围和限制见[审计记录](docs/reports/2026-09-26-local-run-artifact-privacy-audit.md)。
- [x] 成功和断言失败回放使用相同结果协议；失败仍保存 summary、trace 与中文 report。

### 达到程度

- [x] 核心 Agent 业务范围（`agent/`、`context/`、`events/`、`tools/`）行覆盖率 92.7%（2110/2275），达到 ≥80%；全 `modules/` 为 76.1%（6313/8085）。覆盖率 JSON 留在本机临时目录，未纳入仓库。
- [x] 安全相关选定路径行覆盖率 96.4%（726/753），达到 ≥95%。口径固定为 `agent/action_state.py`、`confirmation.py`、`pending_intent.py`、`plan.py`、`plan_actions.py`、`plan_flow.py`、`task_state.py` 与 `tools/navigation.py`、`registry.py`、`validation.py`。
- [x] 离线测试命令排除标记为 `online`/`hardware` 的用例；A6 历史验收批次 625 passed、2 deselected，本轮道路类别审计分支含新增审查回归后为 633 passed、2 deselected。
- [x] 本轮新增缺陷均有对应回归测试；历史已修复缺陷由其原测试覆盖。
- [ ] 任意图表和简历数字可以从保存的结果文件重新生成；部分历史在线原始 trace 位于本机忽略目录，公开仓库不是完整可复算数据包。

## 8. GitHub 与作品集呈现

### 中文介绍与演示（2026-09-23 已完成）

- [x] 将 README 改为中文项目入口，提供可复制的离线回放与报告查看命令，并说明录制观测与真实视频的区别。
- [x] 补充中文舱内感知、道路感知和项目证据边界说明。
- [x] 将回放报告、示例场景、终端交互和运行卡的主要展示文案改为中文，保留机器协议原样。
- [x] 增加失败断言、过期/无效质量状态及终端诊断的中文展示测试。
- [x] 报告技术指标显示为“英文（中文含义）”，并恢复 README 首页舱内、舱外 GIF 直显；旧运行结果保留，新报告使用独立运行标识。

作品集优先级已调整：先让 GitHub 首页直观展示现有 Demo、架构、技术栈与已核验证据，再补能支撑简历的少量必要量化结果；暂停把大规模候选场景扩充当作展示主线。README 中的 Agent 数字只能称为 AI 自审内部评测；感知准确率尚未评估，性能仅展示已实测结果，其他未测指标不放占位成绩。

- [x] 按 PaperBase 式信息层级重排中文 README，保留舱内外 GIF 直显，加入干净离线回放的真实报告截图、能力表、技术栈与证据边界。

### 下一阶段：招聘展示闭环（先完成，不按周排期）

- [x] **首页业务故事**：README 首屏用一个具体驾驶场景说明“离线视频经感知模块生成观测，或直接回放录制观测 → 统一上下文 → 风险事件 → Agent 决策 → 用户确认 → 模拟车机动作”，并给出可点击的 Demo、架构、结果入口。验收：首次访问者无需读源码，在 30 秒内能说出系统解决的问题、两条输入路径和模拟车控边界；截图与 GIF 标明真实视频推理或录制观测回放，不混作同一次运行。
- [x] **可自行启动的中文演示**：校验全新 clone 的安装说明、无密钥离线 Demo 命令、报告打开方式和可选在线 Agent 演示命令；给出示例输入、预期关键画面/状态和常见失败提示。验收：按 README 从零运行，能稳定生成包含舱内/舱外、事件、确认与工具结果的中文报告；离线路径无需 API Key、摄像头或模型权重，在线路径明确费用与 `.env` 要求。
- [x] **业务模块 × 技术栈 × 个人贡献说明**：逐模块列出输入、输出、所解决的业务问题、核心技术与项目内实现；覆盖 OpenCV/MediaPipe 舱内感知、ONNX Runtime/预训练道路模型、上下文质量与事件、Qwen/GLM Agent、工具注册与确认门、回放及测试。验收：README 的技术栈表能直接区分“项目内实现/适配/第三方预训练资产”，不把模型权重或通用库能力写成自研算法；每个模块可链接到代码或中文技术说明。
- [x] **展示证据闭环**：首页保留一条成功链路和至少一条可复现失败链路，Agent 指标链接到 40 场景 × 3 次的中文内部评测报告，关键主张链接到场景、配置、测试或报告。验收：展示通过数同时说明分母、AI 自审/辅助审核、合成观测和模拟车机限制；不把开发变体计入金标，不提前展示尚未完成的舱内外精度或性能数字；公开页面不含密钥、个人轨迹或本机绝对路径。
- [x] **一页面试讲述卡**：用中文写出业务场景、架构取舍、个人负责模块、技术难点、一次失败定位与修复、可信数字及局限，并给出 60–90 秒口述提纲。验收：卡片中每项可量化主张都能回链到 README、报告或可复现命令；明确系统是离线视频/模拟车机原型，不称“量产级自动驾驶”。

### 展示闭环之后再补

- [x] **在线 Agent 单次决策展示**：从真实 Qwen/GLM 请求轨迹生成中文 `report.html`，首屏展示录制舱内外观测及独立演示 GIF、实际送入模型的上下文摘要、模型回复/工具请求、确认门与模拟车机结果；原始 JSON 收入折叠证据区。提供 `SHOWCASE01` 跨域候选场景，明确 GIF 非同步画面、非实时视频推理、非独立人工金标。
- [ ] **真实视频到在线 Agent 的同次运行展示**：将当次视频感知输出接入同一决策报告，并分别标记实际观测、上下文选择、在线模型和模拟工具；当前类别语义未核验，且在线请求含真实场景与工具上下文，待获准外发边界和安全呈现方案后再运行，不以类别识别作为主要结论。

- [x] 将无标签视频核验的真实样本数、处理帧数和性能指标补进 README；当前没有真值标签，因此准确率与典型误检/漏检案例仍不展示。
- [x] 将道路与舱内性能实测结果及硬件、环境、配置和计时口径说明补进 README 与关联报告；两域数据均为本机离线处理，不外推为实时或准确率结论。
- [ ] 若后续获得来源与许可已核验的现成标签，在不进行人工标注的前提下，将准确率与典型失败案例补进 README；获得标签前不放占位成绩。
- [ ] 发布 `v1.0.0` GitHub Release，附可公开的配置、结果摘要、复现说明与许可证边界；大型模型和视频不进入 Git 历史。
- [ ] 在现有页面和复现链路稳定后，再决定是否制作 60–90 秒主演示视频及失败恢复/确认短片；视频不阻塞本阶段验收。

## 9. 明确不做或后置

- [x] 已按先完成离线 Demo、再开展在线 Agent 内部评测的顺序推进；后续感知小样本验证按第 6 节独立实施。
- [ ] 不在安全确认门完成前连接真实车辆控制接口。
- [ ] 不优先开发完整自动驾驶规划器、真实 CAN 控制或大规模仿真平台。
- [ ] 不为了展示技术数量同时引入 ROS2、完整 SLAM、LiDAR 融合和 VLA 训练。
- [ ] 不只保留成功 Demo；失败案例和限制必须进入最终报告。
- [ ] 可选 TensorRT、多摄像头、深度/BEV、ROS2 和仿真增强仅在核心证据闭环完成后评估。
