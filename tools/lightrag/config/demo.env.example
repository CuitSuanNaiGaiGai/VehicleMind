# 服务进程使用根目录 .env 的 DASHSCOPE_API_KEY、QWEN_MODEL、QWEN_BASE_URL。
# 本文件只说明固定实例，不存储密钥。实际端口与目录以 modules/config/knowledge.yaml 为准。
PROFILE=vehiclemind_demo
HOST=127.0.0.1
PORT=9622
WORKING_DIR=runs/lightrag/vehiclemind_demo/storage
