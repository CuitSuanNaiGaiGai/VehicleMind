# A2 策略独立评测集

三条冻结场景分别检验可信高风险事件触发、舱内观测失效后策略拒绝高风险事件、以及冷却抑制与敏感动作显式确认。A2-02 的 `policy_probe` 是观测失效后的确定性策略入口探针，不是 EventDetector 自然发出的新告警。案例哈希由 `EvaluationCase.sha256` 计算，`load_policy_cases` 在运行前逐条核对。建议适配率仅统计实际生成文本的可信 `TRIGGERED` 建议；调度原因序列另行检查。该数据集为内部 AI 自审材料，不代表独立人工金标。
