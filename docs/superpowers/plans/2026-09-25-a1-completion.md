# A1 完成与验收计划

目标：完成已有 A1 清单，保持 API 协议兼容；不扩展到 A2–A6。

技术栈：Python dataclass/Enum、YAML、现有 Qwen/GLM SDK、pytest、JSON trace、中文 HTML。

按当前功能分支直接实施。沿用已批准设计，不新增工作目录或依赖。

- [x] 调用控制：新增 `agent/budget.py`（总时间/工具次数/重复签名预算），将循环抽到 `agent/turn.py`；每次调用前后检查截止时间，在线客户端接收剩余 timeout。测试覆盖超时、重复、工具次数越界和无效 JSON。
- [x] 工具异常：参数必须是合法 JSON 对象且符合现有简单 schema；只读工具明确标记。写操作超时/不明异常禁止自动重放，通过对应状态读取工具查询；不能确认则等待输入并保留不确定性。故障注入测试验证写次数和查询记录。
- [x] 多轮任务：等待输入、过期、取消、目标变更可追溯；短指代关联目标但不绕过显式确认。提供带来源/时间/质量的用户/历史/工具证据，观测仍逐字段追溯。任务记录进入在线 trial 和离线 replay trace；澄清中途切换话题会新建任务。
- [x] 展示：新增独立 HTML 任务/输入证据组件，展示每轮实际上下文、中文状态和错误原因；保留 GIF 与折叠原始 JSON。测试报告转义与实际记录，不编造决策解释。
- [x] 验收：全量 pytest、Ruff/mypy、文件大小检查通过；Qwen/GLM 各跑 C03/R03/R06/X05 并保存真实回复、输入、失败及 AI 辅助审核。不以定向小样本声称总体成功率提升。
- [x] 文档：逐项回看 A1，保存验收报告和启动配置；更新 todolist，提交并推送当前分支。

边界：超时预算是协作式截止检查，SDK 自带网络 timeout；无法强制取消任意同步扩展工具。车机仍为模拟。金标准仍为内部 AI 自审，不做消融，不扩充感知模型。
