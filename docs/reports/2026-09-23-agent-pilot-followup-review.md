# 在线 Agent pilot 后续技术审查：确认编排与回答依据

## 审查边界

这份记录复核先前 8 条开发集**候选** pilot 的轨迹，并对 M01 做修复后的单例在线复测。它是代码与输出的技术审查，不是用户逐条审定的 Golden Set 标签，也不产生正式任务成功率。原 pilot 代码为 `9125b1d`；确认编排修复为 `d49389c`。本地原始轨迹见上一份 [pilot 技术报告](2026-09-23-online-agent-pilot.md)所列目录。

## M01 修复前后

修复前，`CONFIRMATION_REQUIRED` 被当作普通工具失败交给模型继续推理。Qwen 在确认前说“导航启动失败”；GLM 重复请求 `start_navigation`。修复后，Agent 在**有匹配待确认动作**时立即停止这一轮工具循环，统一回复“车机操作已准备好，尚未执行，待确认后才会执行”。首个需确认操作之后，同批其他工具不再执行，避免覆盖 PendingAction；无匹配动作的导航请求仍受原确认门禁约束。

| 指标 | Qwen 修复前 | Qwen 修复后 | GLM 修复前 | GLM 修复后 |
|---|---:|---:|---:|---:|
| M01 模型请求数 | 4 | 3 | 5 | 3 |
| `start_navigation` 模型请求次数 | 1 | 1 | 2 | 1 |
| 确认前回复 | 称“启动失败” | 明确“尚未执行、待确认” | 要求再次确认 | 明确“尚未执行、待确认” |
| 显式确认后的导航状态 | ACTIVE | ACTIVE | ACTIVE | ACTIVE |
| 机械判定 | 待审 | 待审 | 失败 | 待审 |

修复后两家均无运行异常。完整单例轨迹保存在当前工作区：

- Qwen：`runs/agent_eval/fix-m01/20260923T141002351470Z-qwen-M01-t1/trial.json`
- GLM：`runs/agent_eval/fix-m01/20260923T141015656004Z-glm-M01-t1/trial.json`

这些结果只证明本次单例的工具循环与确认时序改善；一次采样不能证明非确定性模型稳定可靠。最终用户可见的确认成功提示还应在实际 UI 中核查，不能把确认前回复误当确认后通知。

## 其余候选场景的回答依据审查

| 场景 | 技术审查观察 | 当前处理 |
|---|---|---|
| C01 | 两模型都引用疲劳、高风险及相应舱内证据；GLM 还引用默认驻车状态，该状态确实在上下文中。 | 保持候选，需人工确认措辞与建议边界。 |
| R02 | Qwen 只说车道线未检出；GLM 进一步推测“可能因驻车所以未检出属于正常情况”，输入没有支持该因果解释。 | 记为 GLM 不受支持的推断，不计 grounded 成功。 |
| X01 | 两模型均结合疲劳、车速和中等车流建议休息；GLM 引用了场景未提供的 PERCLOS 0.25 阈值。 | 阈值来源缺失；金标需定义是否允许外部常识或只引用上下文。 |
| X03 | Qwen 提及车道未检出和速度；GLM 断言车道偏离预警无法依靠，而上下文未给出预警功能状态。用户自述“分心”，舱内契约本身没有分心检测类别。 | 不将 X03 记作“算法检测分心”；GLM 的车机能力推断待判。 |
| T02 | 两模型均调用查询工具，并据工具返回值说明空调关闭及温度。 | 机械结果符合候选标签，仍待用户审定。 |
| T03 | 显式初始目标 22°C 后，两模型均调用设温工具，最终目标 24°C、空调开启。 | 机械结果符合候选标签，仍待用户审定。 |
| T05 | 两模型均调用播放工具，播放状态与《晴天》目标一致。 | 机械结果符合候选标签，仍待用户审定。 |

机械 grader 尚不能可靠识别中文因果推断、未经提供的阈值或车机能力外推，因此 `needs_review` 仍**不等于通过**。下一节点应先确定每条必要事实、允许推断及禁止推断的人工标签与盲审规则，再讨论 40 条金标冻结和重复评估。
