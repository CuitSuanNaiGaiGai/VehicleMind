# 舱内外视频零标注自动核验（本机实测）

## 结论

在本机 macOS arm64、离线视频和真实模型权重条件下，舱内 26/26 条、舱外 26/26 条视频完成逐帧处理，共 52,872/52,872 帧产生有效结构化输出。这证明当前视频与模型管线可处理，不代表感知正确率。全部样本尚无可对齐的人工或官方真值，精度状态为 `not_evaluated`。

| 域 | 成功 / 尝试视频 | 有效输出 / 处理帧 | 输出行为（仅描述模型输出） |
|---|---:|---:|---|
| 舱内 | 26 / 26 | 30,063 / 30,063 | `DROWSY` 13,422 帧、`NORMAL` 9,843 帧、`SUSPECTED` 3,617 帧、`WARMING_UP` 3,027 帧、`UNKNOWN` 154 帧 |
| 舱外 | 26 / 26 | 22,809 / 22,809 | 车道输出 22,069 帧、可行驶区输出 21,777 帧；相邻帧输出变化分别为 157 / 90 次 |

**关键异常：**舱外目标类别汇总只出现 `truck`，共 182,970 次逐帧目标输出。这不是 182,970 个独立目标，更不能视作正确检测数。类别分布过于集中，必须继续核对 YOLOPv2 权重对应的类别表、解码和后处理；在查清前，不用该结果宣传车辆类别识别能力。舱内状态、车道和可行驶区也只是模型输出频次，不能解释为事件真值、召回率、误报率或准确率。

## 可复核条件

- 本地运行 ID：`20260924T104229Z-e44ace9a`；`.complete` 已存在。原始 `manifest.json`、52 个逐视频 JSON、`summary.json` 和中文 HTML/Markdown 位于被 Git 忽略的本地 `runs/perception_audit/<运行 ID>/`，不会上传视频、人脸画面或逐视频时序记录。
- 运行代码提交：`775f4dd401488e0a4df31bc6e46dfd171dda2b94`；运行时 Git `dirty=false`。两侧各 26 条，按文件名冻结清单；52 条的前后文件哈希一致，未发现重复哈希、帧数差异或时间戳回退。
- 舱内模型 SHA-256：`64184e229b263107bc2b804c6625db1341ff2bb731874b0bcc2fe6544e0bc9ff`；舱外模型 SHA-256：`9d81ce2616f84f2c370cb39d4ac825aaf566c488935b52822ef7540d4f0c6e7d`。
- MediaPipe `0.10.35`、ONNX Runtime `1.30.0`；26 条舱外视频的实际会话 provider 均为 `CoreMLExecutionProvider` + `CPUExecutionProvider`，不能称纯 CoreML。舱内时序使用视频时间戳；本次没有时间戳回退。
- 本机沙箱会使 MediaPipe 初始化 macOS 图形上下文时进程直接退出；正式运行在获准的本机非沙箱进程中完成。首次失败的尝试不进入本次 `.complete` 汇总。

## 边界与下一步

来源和许可状态仍是 `unknown`，不能假定这些视频属于某一公开数据集或具有可再发布授权。多个视频/帧不保证来自独立驾驶员或独立道路场景。无人工标注的前提下，本阶段只接受“处理覆盖”和“输出分布”两类结论。下一步优先排查舱外目标单类别输出，再决定是否选用来源明确、与任务对齐的官方标签；不把视频名、模型预测或碰撞标签当金标准。

复现命令与本地报告查看方法见 [README](../../README.md#真实视频与在线-agent)。
